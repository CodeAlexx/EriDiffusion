---
job: extension
config:
  # this name will be the folder and filename name
  name: "rachv1Sd3"
  process:
    - type: 'sd_trainer'
      # root folder to save training sessions/samples/weights
      training_folder: "output"
      # uncomment to see performance stats in the terminal every N steps
#      performance_log_every: 1000
      device: cuda:0
      # if a trigger word is specified, it will be added to captions of training data if it does not already exist
      # alternatively, in your captions you can add [trigger] and it will be replaced with the trigger word
      trigger_word: "rachv1Sd3"
      network:
        type: "lokr"
        lokr_full_rank: true
        lokr_factor: 4
        linear: 64
        linear_alpha: 64
      save:
        dtype: float16 # precision to save
        save_every: 250 # save every this many steps
        max_step_saves_to_keep: 4 # how many intermittent saves to keep
        push_to_hub: false #change this to True to push your trained model to Hugging Face.
        # You can either set up a HF_TOKEN env variable or you'll be prompted to log-in         
#       hf_repo_id: your-username/your-model-slug
#       hf_private: true #whether the repo is private or public
      datasets:
        # datasets are a folder of images. captions need to be txt files with the same name as the image
        # for instance image2.jpg and image2.txt. Only jpg, jpeg, and png are supported currently
        # images will automatically be resized and bucketed into the resolution specified
        # on windows, escape back slashes with another backslash so
        # "C:\\path\\to\\images\\folder"
        - folder_path: "/home/alex/ai-toolkit/datasets/rachealcook"
          caption_ext: "txt"
          caption_dropout_rate: 0.00  # will drop out the caption 5% of time
          shuffle_tokens: false  # shuffle caption order, split by commas
          cache_latents_to_disk: true  # leave this true unless you know what you're doing
          resolution: [ 1024 ]  # flex enjoys multiple resolutions
      train:
        batch_size: 4
        # IMPORTANT! For Flex, you must bypass the guidance embedder during training
        bypass_guidance_embedding: false
        
        steps: 4000  # total number of steps to train 500 - 4000 is a good range
        gradient_accumulation: 1
        train_unet: true
        train_text_encoder: false  # probably won't work with flex
        gradient_checkpointing: true  # need the on unless you have a ton of vram
        noise_scheduler: "flowmatch" # for training only
        optimizer: "adamw8bit"
        lr: 5e-05
        # uncomment this to skip the pre training sample
#        skip_first_sample: true
        # uncomment to completely disable sampling
#        disable_sampling: true
        # uncomment to use new vell curved weighting. Experimental but may produce better results
        linear_timesteps: true

        # ema will smooth out learning, but could slow it down. Recommended to leave on.
        ema_config:
          use_ema: true
          ema_decay: 0.99

        # will probably need this if gpu supports it for flex, other dtypes may not work correctly
        dtype: bf16
      model:
       # huggingface model name or path
        name_or_path: "stabilityai/stable-diffusion-3.5-large"
        is_v3: true
        quantize: true  # run 8bit mixed precision
        max_grad_norm: 0.01
        t5_max_length: 154
        snr_gamma: 5
        
      sample:
        sampler: "flowmatch" # must match train.noise_scheduler
        sample_every: 250 # sample every this many steps
        width: 1024
        height: 1024
        prompts:
         
          - "rachv1Sd3, A woman is wearing a red and gold dress. She is sitting on a bench and playing a stringed instrument. There is a large moon behind her,masterpiece,best quality,(8k, RAW photo, best quality, masterpiece:1.2),(realistic, photo-realistic:1.37),stunning 8k fashion photo"

        neg: ""  # not used on flex
        seed: 42
        walk_seed: true
        guidance_scale: 4
        sample_steps: 25
# you can add any additional meta info here. [name] is replaced with config name at top
meta:
  name: "[name]"
  version: '1.0'
